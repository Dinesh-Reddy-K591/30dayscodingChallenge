class Solution {
    public void moveZeroes(int[] nums) {
        int digits=0;
        for(int i=0;i<nums.length;i++){
            if(nums[i]!=0){
                nums[digits++]=nums[i];
            }
        }
        while(nums.length>digits){
            nums[digits++]=0;
        }
    }
}